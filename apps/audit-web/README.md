# Audit Web

Hot-reloadable React workbench for the binary audit platform.

## Commands

```bash
npm install
npm run dev
npm run lint
npm test -- --run
npm run build
```

The current workbench uses structured local mock data that mirrors the product `/api/*` contracts. It does not require the Python mock API to run.
